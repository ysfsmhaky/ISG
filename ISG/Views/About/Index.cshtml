@model IEnumerable<ISG.Models.About>

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.metin1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.metin2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.metin3)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.metin1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.metin2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.metin3)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
